# 🎓 Система антиплагиата БГУИР

Веб-приложение для проверки документов на плагиат с использованием алгоритмов машинного обучения.

## ✨ Возможности

- 📄 Загрузка PDF и DOCX документов с автоматическим извлечением текста
- 🤖 Анализ плагиата с использованием sentence-transformers
- 🚀 Асинхронная обработка через Celery
- 🧠 Векторизация текстов для поиска похожих документов
- 🔍 Детальный анализ схожести с множественными метриками

## 🏗️ Технологии

- **Backend:** Django 4.2.9, Gunicorn
- **ML/NLP:** PyTorch 2.5.1, sentence-transformers 2.2.2
- **База данных:** PostgreSQL + pgvector
- **Очереди:** Celery + Redis
- **Векторизация:** paraphrase-MiniLM-L6-v2

## 🚀 Быстрый старт

### 1. Подготовка

```bash
# Создайте .env файл из шаблона
cp env.template .env

# Отредактируйте .env (минимум SECRET_KEY и POSTGRES_PASSWORD)
# SECRET_KEY=ваш-секретный-ключ-минимум-50-символов
# POSTGRES_PASSWORD=ваш-пароль
```

### 2. Запуск

```bash
# Собрать и запустить все сервисы
docker-compose up --build

# Или в фоновом режиме
docker-compose up --build -d
```

### 3. Создание суперпользователя

```bash
# В новом терминале
docker-compose exec web python Folder/manage.py createsuperuser
```

### 4. Доступ

- **Приложение:** http://localhost:8080
- **Admin панель:** http://localhost:8080/admin
- **Flower (Celery):** http://localhost:5555

## 📋 Основные команды

```bash
# Запуск
docker-compose up -d

# Остановка
docker-compose down

# Просмотр логов
docker-compose logs -f web
docker-compose logs -f celery

# Применить миграции
docker-compose exec web python Folder/manage.py migrate

# Django shell
docker-compose exec web python Folder/manage.py shell

# Перезапуск сервиса
docker-compose restart web
```

## 🔧 Требования

- Docker Desktop (Windows/Mac) или Docker + Docker Compose (Linux)
- ~5GB свободного места на диске
- Порт 8080 свободен (или измените в docker-compose.yml)

## 📦 Размер Docker образа

Оптимизированный образ весит ~2.5-3.0 GB (используется multi-stage build и CPU-only PyTorch).

## 🗂️ Структура проекта

```
.
├── Dockerfile              # Оптимизированный Docker образ
├── docker-compose.yml      # Конфигурация сервисов
├── entrypoint.sh          # Скрипт инициализации контейнера
├── env.template           # Шаблон переменных окружения
├── requirements.txt      # Python зависимости
└── Folder/               # Django проект
    ├── app/              # Настройки приложения
    ├── documents/        # Основное приложение
    └── users/            # Управление пользователями
```

## 🔒 Переменные окружения

Основные переменные в `.env`:

- `SECRET_KEY` - секретный ключ Django (обязательно!)
- `POSTGRES_PASSWORD` - пароль для PostgreSQL (обязательно!)
- `POSTGRES_DB` - имя базы данных (по умолчанию: plagiarism_db)
- `POSTGRES_USER` - пользователь БД (по умолчанию: postgres)
- `DEBUG` - режим отладки (по умолчанию: False)
- `ALLOWED_HOSTS` - разрешённые хосты

## 🐛 Решение проблем

### Контейнер не запускается

```bash
# Проверьте логи
docker-compose logs web

# Проверьте что .env файл создан
ls .env

# Пересоберите образы
docker-compose build --no-cache
```

### База данных не подключается

```bash
# Проверьте что БД контейнер запущен
docker-compose ps db

# Пересоздайте volumes (⚠️ потеряете данные!)
docker-compose down -v
docker-compose up -d
```

### Celery не работает

```bash
# Проверьте Redis
docker-compose exec redis redis-cli ping

# Перезапустите Celery
docker-compose restart celery
```

## 📝 Лицензия

Все права защищены © БГУИР 2025

